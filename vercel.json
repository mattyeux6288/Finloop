{
  "installCommand": "npm install",
  "buildCommand": "rm -rf packages/shared/dist packages/engine/dist && npm run build -w packages/shared && npm run build -w packages/engine && cd packages/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...route]].ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/[[...route]]" },
    { "source": "/((?!assets|api|vite\\.svg).*)", "destination": "/index.html" }
  ]
}
