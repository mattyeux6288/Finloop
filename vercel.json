{
  "installCommand": "npm install",
  "buildCommand": "npx vite build --config packages/web/vite.config.ts --root packages/web",
  "outputDirectory": "packages/web/dist",
  "functions": {
    "api/[[...route]].ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...route]]" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
