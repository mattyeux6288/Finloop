{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w packages/shared && npm run build -w packages/engine && cd packages/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...route]].ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...route]]" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
