{
  "installCommand": "npm install",
  "buildCommand": "cd packages/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...route]].ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...route]]" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
