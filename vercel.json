{
  "installCommand": "npm install",
  "buildCommand": "rm -rf packages/shared/dist packages/engine/dist packages/server/dist && npm run build -w packages/shared && npm run build -w packages/engine && npm run build -w packages/server && cd packages/web && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...route]].ts": {
      "memory": 1024,
      "maxDuration": 60,
      "includeFiles": "node_modules/csv-parse/**,node_modules/pdf-parse/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/[[...route]]" },
    { "source": "/((?!assets|api|vite\\.svg).*)", "destination": "/index.html" }
  ]
}
